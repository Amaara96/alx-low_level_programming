#include "main.h"

/**
 * cap_string - capitalizes everey word of a string
 * @s: string to modify
 *
 * Return: the resulting string
 */
char *cap_string(char *s)
{
	int i;
	char sp[13] = {' ','\t','\n',',','.','!','?','"','(',')','{','}'}

	for(i = 0; s[i] != '\0', i++)
	{
		if (i == 0 && s[i] 
}

