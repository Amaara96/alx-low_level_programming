#include <stdio.h>
/**
  * main - a program that prints the uppercase and lowercase alphabets
  * followed by a newline
  * Return: Always return to 0
*/
int main(void)
{
	int letters;

	for (letters = 'a'; letters <= 'z'; ch++)
		putchar (ch);

	for (letters = 'A'; letters <= 'Z'; letters++)
		putchar (ch);

		putchar('\n');

		return (0);
}
