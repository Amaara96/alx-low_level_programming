#include "main.h"

/**
 * binary_to_unit - This is a function that converts binary number
 * To unsigned int
 * 
 * @b: This is the pointer to the string
 *
 * @NULL: This is the return value for nothing
 *
 * @Return: It returns to the value
*/

unsigned int binary_to_unit(const char *b)

	char b = 0;
	unsigned int i = 0;

	if b = NULL
	{
		return (i);
	
	}
	else if (char b = char++)
	{
		return (i);
	}
	else
	{
		return (0);
	}

